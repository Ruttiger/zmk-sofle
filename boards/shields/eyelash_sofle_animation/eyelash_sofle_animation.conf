# Disable nice_view's own custom_status_screen.c so our implementation
# (custom_status_screen.c in this shield) is the only one linked.
# nice_view still provides the hardware driver (LS0XX SPI) and sets
# ZMK_DISPLAY_STATUS_SCREEN_CUSTOM via the choice in its Kconfig.defconfig.
CONFIG_NICE_VIEW_WIDGET_STATUS=n
